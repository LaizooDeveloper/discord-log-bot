<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Server <%= guildId %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
  <div class="container">
    <a class="navbar-brand" href="/">Log Control Panel</a>
  </div>
</nav>

<div class="container">
    <div class="card shadow-sm">
        <div class="card-body">
            <h3 class="card-title mb-4 text-center">Edit Settings for Server <%= guildId %></h3>
            <form method="POST">
                <div class="mb-3">
                    <label class="form-label">Member Join Channel ID</label>
                    <input type="text" class="form-control" name="memberJoin" value="<%= logChannels.memberJoin %>">
                </div>

                <div class="mb-3">
                    <label class="form-label">Member Leave Channel ID</label>
                    <input type="text" class="form-control" name="memberLeave" value="<%= logChannels.memberLeave %>">
                </div>

                <div class="mb-3">
                    <label class="form-label">Message Delete Channel ID</label>
                    <input type="text" class="form-control" name="messageDelete" value="<%= logChannels.messageDelete %>">
                </div>

                <div class="mb-3">
                    <label class="form-label">Message Update Channel ID</label>
                    <input type="text" class="form-control" name="messageUpdate" value="<%= logChannels.messageUpdate %>">
                </div>

                <div class="mb-3">
                    <label class="form-label">Role Update Channel ID</label>
                    <input type="text" class="form-control" name="roleUpdate" value="<%= logChannels.roleUpdate %>">
                </div>

                <div class="mb-3">
                    <label class="form-label">Channel Update Channel ID</label>
                    <input type="text" class="form-control" name="channelUpdate" value="<%= logChannels.channelUpdate %>">
                </div>

                <button type="submit" class="btn btn-success w-100">Save Settings</button>
            </form>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
